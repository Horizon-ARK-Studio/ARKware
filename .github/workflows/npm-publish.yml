name: Publish to npm

# This branch (npm) is the package root -- no android-project/, no docs/,
# no shared history with main. Every push here publishes to npm so
# `npm install @horizon-ark-studio/arkware` always tracks the branch tip,
# no separate tagging step required. (Tag pushes still work too, for
# workflow_dispatch-style manual dist-tag control -- see below.)
#
# Because this fires on every push, not just version bumps: if
# package.json's version wasn't bumped since the last successful
# publish, npm rejects the publish (can't republish an existing
# version) and this job goes red. That's expected -- bump the version
# as part of the commit you want published.

on:
  push:
    branches:
      - npm
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      dist_tag:
        description: "npm dist-tag to publish under"
        required: false
        default: "latest"

permissions:
  contents: read
  id-token: write # npm provenance

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout npm branch
        uses: actions/checkout@v4
        with:
          ref: npm

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          registry-url: "https://registry.npmjs.org"

      - name: Ensure npm supports OIDC trusted publishing (needs 11.5.1+)
        run: npm install -g npm@latest

      - name: Verify tag matches package.json version
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          TAG="${GITHUB_REF#refs/tags/v}"
          PKG_VERSION="$(node -p "require('./package.json').version")"
          if [ "$TAG" != "$PKG_VERSION" ]; then
            echo "::error::tag v$TAG does not match package.json version $PKG_VERSION"
            exit 1
          fi

      - name: Install
        # No package-lock.json is committed (deps are minimal), so `npm ci`
        # isn't available -- plain install, and skip lifecycle scripts
        # since `prepare` calls sync-from-main.js, which this CI run
        # doesn't need (publish ships repo contents as-is).
        run: npm install --ignore-scripts

      - name: Publish
        # Trusted Publishing (OIDC) -- no token. npm exchanges the
        # id-token this job's `permissions` block grants for a short-lived
        # publish credential, matched against the Trusted Publisher entry
        # configured on the package at npmjs.com (org/repo/workflow
        # filename must match this repo exactly). Requires the package to
        # already exist on the registry -- see README for the one-time
        # manual bootstrap publish this replaces.
        run: >
          npm publish --access public --provenance
          ${{ github.event.inputs.dist_tag && format('--tag {0}', github.event.inputs.dist_tag) || '' }}

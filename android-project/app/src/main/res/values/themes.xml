<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
      Material 3, DayNight-aware, with Material You dynamic color
      wired up via ArkTubeApplication (DynamicColors.
      applyToActivitiesIfAvailable). On Android 12+ this gets
      overridden at runtime with a wallpaper-derived palette;
      everywhere else it falls back to the branded roles below.

      NoActionBar because the WebView fills the entire window (see
      MainActivity.kt) - there is no toolbar/chrome, same as the
      desktop shell has no browser chrome around the YouTube page.
    -->
    <style name="Theme.ArkTubeApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/md_theme_primary</item>
        <item name="colorOnPrimary">@color/md_theme_onPrimary</item>
        <item name="colorPrimaryContainer">@color/md_theme_primaryContainer</item>
        <item name="colorOnPrimaryContainer">@color/md_theme_onPrimaryContainer</item>
        <item name="colorSecondary">@color/md_theme_secondary</item>
        <item name="colorOnSecondary">@color/md_theme_onSecondary</item>
        <item name="android:colorBackground">@color/md_theme_background</item>
        <item name="colorOnBackground">@color/md_theme_onBackground</item>
        <item name="colorSurface">@color/md_theme_surface</item>
        <item name="colorOnSurface">@color/md_theme_onSurface</item>
        <item name="colorSurfaceVariant">@color/md_theme_surfaceVariant</item>
        <item name="colorOnSurfaceVariant">@color/md_theme_onSurfaceVariant</item>
        <item name="colorOutline">@color/md_theme_outline</item>

        <item name="android:statusBarColor" tools:targetApi="21">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="23">true</item>
        <item name="android:navigationBarColor" tools:targetApi="27">?attr/colorSurface</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">true</item>
    </style>

    <!--
      Splash screen theme (androidx.core.splashscreen backport -
      works API 24+, not just the native API 31+ SplashScreen).
      Applied to MainActivity in AndroidManifest.xml; MainActivity
      calls installSplashScreen() before super.onCreate(), which
      swaps this theme for postSplashScreenTheme once the WebView
      starts rendering. Icon source: mipmap/ic_launcher_foreground
      (derived from the repo's appIcon.png, see
      mipmap-xxxhdpi/ic_launcher_foreground.png).
    -->
    <style name="Theme.ArkTubeApp.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.ArkTubeApp</item>
    </style>
</resources>

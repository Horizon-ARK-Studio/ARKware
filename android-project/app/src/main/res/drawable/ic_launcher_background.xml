<?xml version="1.0" encoding="utf-8"?>
<!-- Adaptive icon background: solid backing color sampled from
     appIcon.png's own corner pixel (the actual brand icon, see
     mipmap-<density>/ic_launcher_foreground.png), so any sliver the
     adaptive-icon mask reveals around the full-bleed foreground photo
     matches seamlessly instead of showing a mismatched color. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#E60406"
        android:pathData="M0,0h108v108h-108z" />
</vector>
